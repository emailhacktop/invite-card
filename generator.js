"use strict";const BASE_URL='https://emailhacktop.github.io/invite-card/invite.html';const container=document.getElementById('list');let allLinks='';function safeCSV(value){const text=String(value);if(text.startsWith('=')||text.startsWith('+')||text.startsWith('-')||text.startsWith('@')){return "'"+text;}return text;}for(const id in guests){const guest=guests[id];const link=BASE_URL+'?id='+encodeURIComponent(id)+'&token='+encodeURIComponent(guest.token);allLinks+=guest.name+'\n'+link+'\n\n';const card=document.createElement('div');card.className='card';const name=document.createElement('div');name.className='name';name.textContent=guest.name;const linkBox=document.createElement('div');linkBox.className='link';linkBox.textContent=link;const button=document.createElement('button');button.textContent='کپی لینک';button.onclick=()=>{navigator.clipboard.writeText(link);alert('لینک کپی شد');};card.appendChild(name);card.appendChild(linkBox);card.appendChild(button);container.appendChild(card);}document.getElementById('copyAllBtn').onclick=()=>{navigator.clipboard.writeText(allLinks);alert('همه لینک‌ها کپی شد');};document.getElementById('csvBtn').onclick=()=>{let csv='ID,NAME,TOKEN\n';for(const id in guests){csv+=safeCSV(id)+','+safeCSV(guests[id].name)+','+safeCSV(guests[id].token)+'\n';}const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='guests.csv';a.click();};